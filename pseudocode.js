// const { default: inquirer } = require("inquirer")

// Install inquirer
// need fs

// build a questions object for inquirer that will get us the correct information

// need a function to generate markdown

// need to append to a file - README.MD

// Use the inquirer.prompt() method to ask the user the questions defined in the questions object
// Pass the responses to the function that generates Markdown content
// Pass the generated Markdown content to the function that appends to the README.md file
